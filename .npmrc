# Next.js and Prisma both rely on hoisted binaries being resolvable from the
# workspace root; keep the default isolated store but allow build scripts for
# the packages that genuinely need them.
enable-pre-post-scripts=true
