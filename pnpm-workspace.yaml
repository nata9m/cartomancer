packages:
  - "apps/*"
  - "packages/*"

# Packages allowed to run install scripts (pnpm 10 blocks them by default).
onlyBuiltDependencies:
  - "@prisma/engines"
  - prisma
  - esbuild
  - "@tailwindcss/oxide"
  - sharp
  - unrs-resolver
