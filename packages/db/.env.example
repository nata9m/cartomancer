# Connection used by the Prisma CLI (migrations, seed, studio) and by the
# generated client. The api and web apps read their own copies of this.
DATABASE_URL="postgresql://cartomancer:cartomancer@localhost:5432/cartomancer?schema=public"
