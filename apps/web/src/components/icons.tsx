/**
 * The mockups use Tabler Icons via a CDN icon font (`ti ti-*`). Production uses
 * the @tabler/icons-react package instead — same glyphs, no external font
 * request — so the icon names below map 1:1 to the mockups' classes.
 */
export {
  IconFlame, // ti-flame
  IconBuildingBank, // ti-building-bank
  IconMap, // ti-map
  IconFlag, // ti-flag
  IconBulb, // ti-bulb
  IconChevronRight, // ti-chevron-right
  IconChevronDown, // ti-chevron-down
  IconArrowLeft, // ti-arrow-left
  IconArrowRight, // ti-arrow-right
  IconX, // ti-x
  IconCheck, // ti-check
  IconLock, // ti-lock
  IconBrandGoogle, // ti-brand-google
  IconBrandApple, // ti-brand-apple
  IconCircleCheck, // ti-circle-check
} from '@tabler/icons-react';
